#!/usr/bin/env node

var FakeCAS = require('../').FakeCAS;

new FakeCAS.Server().start(function(err) { if(err) console.error(err); });
// new FakeCAS.WebApp().start(function(err) { if(err) console.error(err); });
